# Installation
To install into an existing CloudTAK deployment:

1. Copy or symlink this repo to `./api/web/plugins/` folder.
2. From the `./api/web` folder, run `npm run build`.
3. If in a Docker container, restart the container.
